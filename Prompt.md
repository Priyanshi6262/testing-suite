## Prompts

1. Ensure components correctly display dynamic data passed through props.

2. Simulate user clicks and verify UI updates accordingly.

3. Test user typing behavior and confirm that input values update correctly.

4. Ensure component state changes reflect accurately in the UI.

5. Create Storybook stories for reusable React components such as Button, Input Field, and Card. Each story should render the component independently with default props.

6. Enable background switching in Storybook to preview components in both light and dark themes using Storybook parameters.

7. Enable background switching in Storybook to preview components in both light and dark themes using Storybook parameters.

8. Guide me to build Storybook as a static site and deploy it using a hosting platform like Vercel, including required configuration files.

9. Show how to test component behavior interactively in Storybook, such as clicking a button or typing into an input field, and verify UI updates.

10. Configure Storybook args (props) for components so that users can dynamically change properties like text, variant (primary/secondary), and disabled state from the controls panel.